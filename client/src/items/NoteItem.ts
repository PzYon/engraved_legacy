import {INoteItem} from "engraved-shared/dist";
import {BaseItem} from "./BaseItem";

export class NoteItem extends BaseItem {
    public constructor(noteItem: INoteItem) {
        super(noteItem);
    }
}

