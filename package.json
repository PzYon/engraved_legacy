{
  "name": "engraved",
  "version": "0.0.2",
  "dependencies": {},
  "scripts": {
    "start": "npm run server:prod",
    "shared:build:prod": "cd shared && npm run publishtonpm",
    "server:build:prod": "cd server && npm install && npm run build",
    "client:build:prod": "cd client && npm install && npm run build && copyfiles build ../server/dist/client",
    "heroku-postbuild": "npm run shared:build:prod && npm run server:build:prod && npm run client:build:prod"
  },
  "devDependencies": {
    "copyfiles": "^2.0.0"
  },
  "engines": {
    "node": "8.11.2"
  }
}
