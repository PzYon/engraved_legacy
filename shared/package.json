{
  "name": "engraved-shared",
  "version": "0.0.102",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "copyPackageJson": "copyfiles package.json dist",
    "dev-shared": "npm run copyPackageJson && tsc -w",
    "lint": "tslint --project tsconfig.json",
    "publishtonpm": "tsc && npm run copyPackageJson && cd dist && npm publish"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.1.1",
    "tslint": "^5.10.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.5.3"
  },
  "engines": {
    "node": "8.11.2"
  }
}
