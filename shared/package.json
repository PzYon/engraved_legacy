{
  "name": "engraved-shared",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-shared": "tsc -w && cp package.json ../dist",
    "copy-packagejson": "copy package.json dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^2.9.2"
  }
}
